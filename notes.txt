
generate the config:

time lkmake ARCH=arm64 CC=clang CROSS_COMPILE=aarch64-linux-gnu- -j8 qemu_defconfig

build:

time lkmake ARCH=arm64 CC=clang CROSS_COMPILE=aarch64-linux-gnu- -j8

run:

qemu-system-aarch64  -m 1024 -machine virt -cpu cortex-a53   -nographic -kernel arch/arm64/boot/Image -append "console=ttyAMA0"